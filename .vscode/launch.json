{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug: build estatico + Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4173",
      "webRoot": "${workspaceFolder}/dist",
      "preLaunchTask": "preview: dist",
      "postDebugTask": "preview: stop"
    }
  ]
}
